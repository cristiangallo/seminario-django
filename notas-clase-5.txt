Obtener URLs del Admin:
from django.urls import reverse
reverse('admin:posts_post_change', args=(post.id,))

https://docs.djangoproject.com/en/5.1/topics/http/urls/#reversing-namespaced-urls
https://en.proft.me/2014/10/12/reversing-admin-urls-django/
<li><a href="{% url 'admin:index' %}">Administrador</a></li>

Corregir urls del menú
<li><a href="/#totop">Home</a></li>
<li><a href="/#totop">Libros Prestados</a></li>
<li><a href="/#totop">Prestar Libro</a></li>
<li><a href="/#totop">Devolver Libro</a></li>

https://docs.djangoproject.com/en/5.1/ref/signals/
cantidad de ejemplares disponibles
 if kwargs.get('signal') == post_save:
    print(f"Se ha actualizado un ejemplar: {instance}")
elif kwargs.get('signal') == post_delete:
    print(f"Se ha eliminado un ejemplar: {instance}")

if kwargs.get('signal') == post_save:
    print(f"Se ha actualizado un ejemplar: {instance}")
elif kwargs.get('signal') == post_delete:
    print(f"Se ha eliminado un ejemplar: {instance}")

Custom Templates tags: https://docs.djangoproject.com/en/5.1/howto/custom-template-tags/
from django import template
register = template.Library()

@register.filter
def get_class_name(obj):
    return obj.__class__.__name__

{{ objeto|get_class_name }}


Buscador de libros

https://sorl-thumbnail.readthedocs.io/en/latest/
agregar Imagen portada libro al modelo

